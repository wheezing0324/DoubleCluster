# CSAHFL: Clustered Semi-Asynchronous Hierarchical Federated Learning
# Requirements file with exact versions for reproducibility
# Python 3.8+ required (3.9 recommended)

# Core Scientific Computing
numpy>=1.21.0,<2.0.0          # Array operations, SVD computation
scipy>=1.7.0                  # Hierarchical clustering, statistics
scikit-learn>=0.24.0          # Additional ML utilities

# Deep Learning Framework
torch>=1.9.0                  # Neural network models, tensors
torchvision>=0.10.0           # Dataset loading (Fashion-MNIST, CIFAR10, SVHN)

# Configuration and Data Handling
pyyaml>=5.4.0                 # Configuration file parsing
pandas>=1.3.0                 # Results dataframes, table generation

# Progress and Logging
tqdm>=4.62.0                  # Progress bars for experiments

# Visualization
matplotlib>=3.4.0             # Plotting for results visualization
seaborn>=0.11.0               # Enhanced visualization

# Optional: For enhanced functionality
# tensorboard>=2.6.0          # Training visualization (optional)
# pytest>=6.2.0               # Unit testing (optional)
# black>=21.0                 # Code formatting (optional)
# flake8>=3.9.0               # Code linting (optional)

# System Requirements:
# - CPU: 8+ cores recommended (parallel client simulation)
# - RAM: 16GB minimum (200 clients with models in memory)
# - GPU: Optional but recommended (NVIDIA GPU with 4GB+ VRAM, CUDA 11.0+)
# - Storage: 10GB for datasets and checkpoints

# Installation:
#   pip install -r requirements.txt
#
# For GPU support (if using NVIDIA GPU):
#   pip install torch>=1.9.0 torchvision>=0.10.0 --extra-index-url https://download.pytorch.org/whl/cu111
